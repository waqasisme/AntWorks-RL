# Docs track reality, not the other way around

During 0.1 review, the exercise's stated package name (`antworks`) didn't match what was actually built (`antworks_rl`, consistent with the `antworks-rl` project name). Rather than rename working code to match a lesson doc, the user updated `CLAUDE.md`'s convention entry to reflect the real package name — correctly applying the repo's own "docs track reality" principle instead of treating the explainer as ground truth.
